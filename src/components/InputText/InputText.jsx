import React from "react";
import "./InputText.css";
const InputText = () => {
  return (
    <input type="text" className="inputText" placeholder="Search Twitter" />
  );
};

export default InputText;
